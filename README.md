# 🤖 ASinglePoint_Bot — Финансовый Telegram-бот

Бот для учета финансов: долги, расходы, доходы, цели накоплений и бюджетирование.

## 🚀 Быстрый запуск

### На локальном компьютере (Mac/Windows/Linux):
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/gokov11/ASinglePoint_Bot.git
cd ASinglePoint_Bot

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Запустите бота
python3 bot.py
```

### На сервере (Ubuntu/Debian) одной командой:
```bash
git clone https://github.com/gokov11/ASinglePoint_Bot.git && cd ASinglePoint_Bot && pip3 install -r requirements.txt && python3 bot.py
```

## ⚙️ Настройка

### 1. Получите токен бота:
- Напишите [@BotFather](https://t.me/botfather) в Telegram
- Создайте нового бота: `/newbot`
- Скопируйте токен (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Вставьте токен в код:
Откройте файл `bot.py` и найдите строку:
```python
API_TOKEN = '8393104234:AAGwcbmK8qlxiIzcJIPIqeo3JAz8tBNuYfo'
```
Замените на ваш токен:
```python
API_TOKEN = 'ВАШ_ТОКЕН_ЗДЕСЬ'
```

## 📁 Структура проекта

- `bot.py` — основной код бота
- `requirements.txt` — зависимости Python
- `.gitignore` — игнорируемые файлы
- `asinglepoint.db` — база данных (создается автоматически)

## 🎯 Основные функции

| Функция | Описание | Команда в боте |
|---------|----------|----------------|
| 📋 **Долги** | Учет кредитов и долгов | "📋 Мои долги" |
| 💸 **Расходы** | Учет расходов по категориям | "💸 Внести расход" |
| 💰 **Доходы** | Учет источников дохода | "💰 Внести доход" |
| 🎯 **Цели** | Накопление на цели | "🎯 Мои цели" |
| 📊 **Бюджет** | Управление бюджетами | "💰 Бюджет" |
| 📈 **Аналитика** | Графики и статистика | "📈 Аналитика+" |
| ⚙️ **Настройки** | Настройка уведомлений | "⚙️ Настройки" |

## 🔧 Требования

- Python 3.8 или выше
- Установленный `pip` (менеджер пакетов Python)

## 🐛 Устранение проблем

### Ошибка: "No module named 'matplotlib'"
```bash
pip install matplotlib
```

### Ошибка: "No module named 'aiogram'"
```bash
pip install aiogram
```

### На Mac с ошибкой GUI для matplotlib:
Добавьте в начало `bot.py`:
```python
import matplotlib
matplotlib.use('Agg')
```

## 🌐 Запуск на сервере 24/7

### Способ 1: В фоновом режиме
```bash
# Запустить в фоне
nohup python3 bot.py > bot.log 2>&1 &

# Посмотреть логи
tail -f bot.log

# Остановить
pkill -f "python3 bot.py"
```

### Способ 2: С systemd (рекомендуется для сервера)
Создайте файл `/etc/systemd/system/asinglepoint.service`:
```ini
[Unit]
Description=ASinglePoint Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/ASinglePoint_Bot
ExecStart=/usr/bin/python3 /путь/к/ASinglePoint_Bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем выполните:
```bash
sudo systemctl daemon-reload
sudo systemctl enable asinglepoint
sudo systemctl start asinglepoint
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что Python установлен: `python3 --version`
2. Проверьте установку зависимостей: `pip list | grep aiogram`
3. Проверьте токен бота в файле `bot.py`

## 📄 Лицензия

Этот проект открыт для использования и модификации.

---

**Счастливого ведения финансов!** 💰✨